`server` is the solution to this exercise. `client` is used for testing

To run, first kick off the server

    $ cd server
    $ go run .


Next, start the client. You should see log files showing traffic being sent

    $ cd ../client
    $ cd run .


